rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Development rules: Allow public read/write for ALL paths
    // ⚠️ WARNING: This is for development only! Update before production!
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}

